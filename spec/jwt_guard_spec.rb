# frozen_string_literal: true

RSpec.describe JwtGuard do
  it "has a version number" do
    expect(JwtGuard::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
